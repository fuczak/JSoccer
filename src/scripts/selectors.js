var selectors = {
	overlay: document.getElementsByClassName('overlay')[0],
	userTeam: document.getElementById('userTeam'),
	teamlist: document.getElementsByClassName('teamlist')[0]
};

module.exports = selectors;
