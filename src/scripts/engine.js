var $ = require('jquery');
var ramjet = require('ramjet');
var tactics = require('./tactics');

var state = {};

var engine = {
  init: init
};

function init() {
  // set initial state
}

module.exports = engine;
